# Living Dashboard visual polish — 2026-09-24

## Starting point and scope

- Inspected a clean clone of real `main` at `b7fb525075cdebe1ab1740c16b32652d6f78b2a0` before editing. Rechecked the remote main SHA before committing; it was unchanged.
- Read the six requested project documents, `AGENTS.md`, current workspace/motion tokens and the installed Next.js CSS guidance.
- `/dashboard` renders `DashboardOverview` → `PageHeader`, `FarmSwitcher`, `StatusCard`, `FarmOverviewCards` → `SummaryCard` / `WeatherDashboard`.
- The older dashboard-layout, dashboard-header, active-property-summary, irrigation-widget, sensor-summary, quick-actions and ai-recommendation components have no active imports. They were neither revived nor edited.
- Isolated the work on `ui/living-dashboard`, based on current main. The completed application redesign is preserved.

## Completed work

- Farm Today keeps the same status branches, copy, schedule values and CTA destinations. Layered agricultural greens, quiet field contours, a restrained gold status accent and a correctly contrasted icon create depth. Ambient motion is limited to a bounded opacity layer and a tiny scheduled-state ring.
- Weather uses a Dashboard-only appearance: clear daylight, cloud, precipitation and night tints come from existing normalized conditions and `isDay`. Temperature, condition, wind and the existing daily rainfall total retain their values/units. The provider's farm-local report/valid time is visible; there is no new claim of a live sensor feed or freshness guarantee.
- Scheduled irrigation presents the existing date, clock time and duration as a calendar event with a timeline cue and localized Scheduled label. There is no countdown, progress estimate, new scheduling rule or valve state. A saved schedule is not reclassified as an active/upcoming execution.
- Crop/garden information gets botanical icon treatment and a quiet secondary surface. Its count remains the original number of plant entries. On wider layouts the crop card forms a compact full-width row beneath operational information.
- Dashboard-only page atmosphere and a warmer first-farm empty state reuse the existing components. Mobile gets a full-width farm selector and hero action. Existing navigation/safe-area spacing stays owned by AppShell.
- Farm changes animate the existing content for the shared 220 ms panel duration. No keyed remount of WeatherDashboard or the farm selector, timers, React animation loops, canvas, blur, animated shadows or new dependency.
- Shared workspace cards gain optional className and semantic presentation slots; defaults remain intact. Dashboard-specific CSS is isolated in CSS modules. Weather/FarmOverviewCards explicitly opt in via `appearance="dashboard"`.
- Focus treatment, direction-aware arrows, logical spacing, `<bdi>` measurements, light/dark tokens and reduced-motion suppression remain supported. All visible copy uses existing translation keys.

## Preserved boundaries

No changes to Auth, MFA, Supabase clients/configuration, RLS, entitlements, schema, cloud persistence, irrigation scheduling, weather fetching/normalization/refresh/fallback, localization architecture, farm selection or navigation destinations/return context. Test fixtures only are synthetic; the product has no demo farms or invented readings.

## Verification evidence

- Existing 189 application tests passed; 10 added regression tests passed. These render the actual components in English/Persian and exercise coordinate/schedule absence, empty farms, gardens, real schedule fields, provider time/condition, regional units, fallback/loading/error states, encoded weather destinations, farm selection URL context, transition replay and reduced-motion opt-out.
- PGlite database checks: 16 passed; one independent-session concurrency test is intentionally skipped. `npm run test:db:native` cannot start in this environment (`EINVAL`); the existing GitHub Foundation workflow remains the native PostgreSQL gate.
- `npm run lint`, `npm run typecheck`, `npm run localization:check`, standard `npm run build` (Turbopack) and all 21 `npm run test:http` checks passed. Localization: both current supported locales, 1,258 keys, no missing keys/usages or detected hardcoded user-facing strings.
- Source review covered mobile/tablet/desktop rules, dark/light tokens, Persian direction, focus, long content, loading-card minimum height, safe areas and reduced-motion selectors. This is not a browser-rendered visual test.

## Remaining visual acceptance

Browser verification was attempted. This environment rejects localhost with `net::ERR_BLOCKED_BY_CLIENT` and disallows file-protocol previews. No browser-policy workaround was used. Authenticated screenshots, rendered 390/768/1440 layouts, actual switch/loading geometry, contrast measurements, reduced-motion rendering and physical iPhone/Safari FPS remain unverified. Do not treat the successful build or server-rendered fixture tests as visual sign-off.

Next: review the draft PR and perform authenticated light/dark, English/Persian, mobile/tablet/desktop visual acceptance before merging.
