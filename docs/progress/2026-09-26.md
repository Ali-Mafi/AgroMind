# General app review fixes

- Continued from production/main `61f45c1d88a52aca2a7ee75e63dac43bfe89e33e` after the user authorized fixing the review findings.
- Corrected stale PWA assertions: standard/auth documents use the native default status bar; Dashboard and Weather retain their route-specific translucent metadata. No production status-bar behavior was changed.
- Added a bounded, sanitized display-preferences cookie. The root renders saved language, direction, units and region on the server; existing localStorage preferences migrate after hydration. No identity, credentials or farm records are stored in it. Cookies/storage being unavailable cannot prevent editing settings.
- Decision/tradeoff: reading preferences makes the public landing dynamically rendered. HTML is private/no-store to prevent language or region leakage between visitors. There is no new auth/database request in the root. Existing devices need one hydrated visit to migrate their localStorage settings before a subsequent full load can use them server-side.
- Fixed the navigation translation collision with the theme's “System” label. Both supported languages now distinguish available farm/weather/scheduling features from planned AI/device control. Marketing previews are explicitly labeled sample data.
- Removed coordinates, resolved place names and upstream exception objects from application location logs. Location responses are private/no-store; blank coordinate strings are rejected before contacting providers.
- Renamed six local migrations to their already-applied hosted versions after comparing their SQL. This is a filename/history alignment only; no hosted migration, schema, data, RLS or permission was changed. Updated the affected test references.
- Documented the server-only `WEATHERAPI_API_KEY` in `.env.example`. The production review found `model-only` Open-Meteo weather, corresponding to a missing/empty key. The Vercel connector has no environment-variable operation, and its settings page requires a separate browser login. No key was invented, exposed, committed or changed. WeatherAPI activation remains pending configuration and redeployment.

## Verification checkpoint

- Passed: lint, typecheck, 228 application tests, 16 PGlite tests, localization (en/fa, zero missing usages), normal production build, and 51 HTTP checks.
- HTTP checks include server-rendered Persian/RTL, English and default HTML for landing/sign-in/sign-up in sequence, private cache isolation, protected-route redirects, public assets and optimized Dashboard images.
- The native-only database race test cannot run locally: unprivileged process startup returns `EINVAL`. GitHub Actions must validate native PostgreSQL before merging.
- Landing JS inventory: 265,263 → 266,251 gzip bytes (+988 bytes). Logo remains 14,602 bytes; no initially hidden reveal was reintroduced. Seven local HTTP runs measured warm median headers around 7 ms before vs 20 ms after. These uncontrolled local timings are not production/iPhone paint or authenticated-navigation measurements.
- Browser access to Vercel recovered after an initial tool-usage limit, then reached its normal sign-in page. No authenticated Vercel session or physical iPhone was available.

## Next step

Publish the fix branch, validate GitHub Actions including native PostgreSQL, then merge and verify the production deployment. Configure the missing WeatherAPI key separately through the authenticated Vercel project settings.
