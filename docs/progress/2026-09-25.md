# SSR and navigation performance regression

- Continued from the real-main/production inspection and navigation/provider
  investigation at `725d27842f91c50328139b6bf968b1516ebd790c`. Did not repeat the
  completed Dashboard image optimization or redesign.
- Working branch: `fix/ssr-navigation-performance`.
- Restored visible SSR Landing, reduced logo from 1,126,025 to 14,602 bytes, scoped
  animation code, deferred first-visit dialog, added workspace-only streaming
  skeletons, and removed duplicate/heavy PWA startup work.
- Landing HTML-referenced JS inventory: 1,073,291 → 858,395 raw bytes; 338,325 →
  265,264 gzip bytes. This is build inventory, not measured iPhone transfer/FCP.
- Preserved the existing auth/data gates and per-route FarmProvider ownership.
  Root providers remain persistent. No private-data cache or security bypass.
- Final checks: lint, typecheck, 221 application tests, 16 PGlite tests, localization,
  normal build and 42 HTTP checks pass. Native-only concurrency test remains
  unexecuted: native PostgreSQL cannot start in this container (`EINVAL`).
- Browser/device/authenticated timing is unavailable; no real iPhone or logged-in
  navigation claim is made. Browser download/startup attempts failed.
- At the local-verification checkpoint, production remained READY on base main
  and no commit/push/PR/deployment had been made under the all-checks-pass gate.
- The user subsequently approved review-only commit/push/PR publication to run
  native PostgreSQL in GitHub Actions. No merge or production deployment is
  authorized; current hosted results belong to the PR/check runs.
- Full findings, measurements, changed files and limits:
  [Performance regression report](../performance-regression-2026-09-25.md).

## Next task

Publish the authorized review-only branch/PR and verify CI/native PostgreSQL and
the Vercel preview, leaving main and production unchanged.
