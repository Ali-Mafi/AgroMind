# UX polish pass

Base: `e13983008ef49744bda7b9feb409a12af00e987d` (PR #8 merged).
Branch: `ui/ux-polish-pass`.

## Verified starting point

- Cloned real `main`; HEAD and Production `agromind.ir` both matched the base above. Vercel deployment `dpl_FAJisAaU8sspj6amaREqjymDqkop` was READY.
- Inspected the existing redesign and the four project guides before editing. Production landing rendered; `/dashboard` redirected to sign-in in the unauthenticated browser.
- Confirmed the reported visible-wizard limit bug, unmirrored Next icon, inverted Send icon, native disclosures and abrupt custom selects in the implementation. Preserved completed PR #8 structure and destinations.

## Completed work

- Shared motion tokens: 160 ms controls, 220 ms popovers/disclosures, 260 ms dialogs, 240 ms theme colors. Transform/opacity for micro-interactions; bounded accordion height is the intentional layout-animation exception. No constant surface animation or new dependency.
- Theme colors start transitioning only after initial hydration. The existing sliding theme pill remains, gains keyboard radio navigation, and respects reduced motion. No spring is added to cards or whole pages.
- Improved the existing PreferenceSelect with Base UI entry/exit states, RTL direction context, viewport collision bounds, stable checkmark spacing and highlighted/selected states. Irrigation type now uses that same accessible control while keeping its four existing values, icons and descriptions. Time picker uses the installed Base UI Popover for focus, Escape, outside dismissal and mobile positioning; its calculations and save behavior are unchanged.
- One reusable Disclosure replaces every native details/summary surface (farm information, help, irrigation controls, forecast source details and hourly values). Lucide indicators rotate; retained panels preserve drafts and data. Base UI supplies expanded state, keyboard activation and hidden-region semantics. WebKit marker suppression is also applied defensively.
- Farm limit presentation returns a dedicated back/title/explanation/current-limit/subscription state. The wizard is completely absent when the existing `canCreateFarm` is false. No entitlement rule changed.
- Direction-aware navigation icons fix wizard Next/Previous and repeated forward/back affordances; Send is never flipped. Form adornments and map overlays use logical spacing. Base UI direction follows the existing locale.
- Farm overview remains status-first, with next-irrigation/crop/soil headings and explicit View forecast / Manage irrigation / View crop details / View sensors actions. Existing query parameters and destinations stay intact.
- Assistant uses a mobile composer above the measured bottom navigation, safe-area spacing, bounded auto-growing textarea and `visualViewport` keyboard insets. Desktop uses an in-flow sticky composer. Starter selection fills/focuses the draft and gives feedback. Sending remains disabled because chat is unavailable.
- Help adds six topic groups, twelve FAQs and a draft-only support form (subject/category/description, validation, explicit unavailable submission). About adds mission, capabilities with honest planned-feature labels, existing `APP.version`, Website and Help links.
- Reviewed Persian workspace copy across home, farms, forms, irrigation, sensors, account, settings, assistant, help/about and feedback. Updated literal/awkward wording, including Preferences → تنظیمات. Dynamic farm, location, crop and plant names remain untouched. Added matching English/Persian coverage for all new copy and placeholder parity tests.
- A build-only compatibility correction wraps the existing onboarding ProtectedLayout in a route layout accepting only `children`. Its default onboarding requirement and authentication behavior remain identical. Webpack's route type checker rejected the pre-existing direct re-export with an optional extra prop.

## Support integration boundary

No ticket backend, ticket API/schema or official support email was found in the repository/configuration/docs. Submission and email actions explicitly remain unavailable. The editable draft is in component memory only; it is neither sent nor persisted. There is no simulated loading, success, ticket ID or fake address. Privacy and Terms routes do not exist and are not linked.

A separate future task must choose a real support service or authenticated ticket endpoint, define ownership/data retention and abuse controls, add reviewed schema/RLS if Supabase storage is chosen, and configure a verified official support address. Network loading, transport errors and success confirmation must be connected to real submission results at that time.

## Preserved boundaries

No changes to auth services, MFA, session security, Supabase configuration, RLS, entitlement logic, data models, farm mutations/persistence, weather providers/calculations, irrigation rules or database behavior. No dependency or lockfile changes. Production is not deployed from this branch; review/merge is separate.

## Validation and limitations

- Lint and TypeScript pass.
- Localization check passes: 1,209 keys, two supported locales, zero missing keys/usages, suspicious untranslated values or detected hardcoded UI strings. Dynamic help topics and placeholders have regression coverage as well.
- 167 application tests pass (14 new polish tests, plus the existing 153). Covers limit/wizard exclusion, English/Persian direction and label order, farm destinations, real help/about links, honest support/AI state, disclosure semantics, assistant starter editing and keyboard-inset calculations.
- PGlite: 15 pass; the independent-connection concurrency case is skipped by design. Native PostgreSQL cannot start in this container's UID mapping (`EINVAL`); the unchanged CI workflow runs it on Ubuntu.
- Production build passes with `npm run build -- --webpack`. Default local Turbopack cannot retrieve Google font assets in this environment; no font/config workaround was committed. CI retains the normal `npm run build` command.
- All 21 local HTTP checks pass, including protected redirects, no-store handling, auth form availability and compatibility routes.
- Responsive CSS reviewed for 320/390/768/1024/1440/1920 layouts, RTL, safe areas, focus, overlays and reduced motion. The cloud browser rejects localhost with `net::ERR_BLOCKED_BY_CLIENT`; Production has no authenticated session. This is NOT a rendered-device or screenshot comparison claim. Physical iPhone/iPad Safari/PWA keyboard, actual animation smoothness/FPS, authenticated visual checks and large-text layout remain manual acceptance checks.

Next: review the PR and CI, then verify the authenticated mobile experience on iOS before merging.
